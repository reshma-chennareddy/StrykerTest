fastapi==0.95.0          # FastAPI framework for building APIs
uvicorn==0.22.0          # ASGI server for FastAPI
httpx==0.24.1            # For making asynchronous HTTP requests (useful for FastAPI)
pytest 
pytest-asyncio
